<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pbad.codegenerator.mapper.DatabaseConfigMapper">

    <resultMap id="BaseResultMap" type="com.pbad.codegenerator.domain.po.DatabaseConfigPO">
        <id column="id" property="id" jdbcType="VARCHAR"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="type" property="type" jdbcType="VARCHAR"/>
        <result column="host" property="host" jdbcType="VARCHAR"/>
        <result column="port" property="port" jdbcType="INTEGER"/>
        <result column="database" property="database" jdbcType="VARCHAR"/>
        <result column="schema" property="schema" jdbcType="VARCHAR"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="created_at" property="createdAt" jdbcType="VARCHAR"/>
        <result column="updated_at" property="updatedAt" jdbcType="VARCHAR"/>
        <result column="created_by" property="createdBy" jdbcType="VARCHAR"/>
        <result column="updated_by" property="updatedBy" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, name, type, host, port, `database` AS `database`, `schema` AS `schema`, username, password, created_at, updated_at, created_by, updated_by
    </sql>

    <!-- 查询所有配置 -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM code_generator_database_config
        ORDER BY created_at DESC
    </select>

    <!-- 根据ID查询配置 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM code_generator_database_config
        WHERE id = #{id}
    </select>

    <!-- 插入配置 -->
    <insert id="insert">
        INSERT INTO code_generator_database_config 
        (id, name, type, host, port, `database`, `schema`, username, password, created_at, updated_at, created_by, updated_by)
        VALUES 
        (#{id}, #{name}, #{type}, #{host}, #{port}, #{database}, #{schema}, #{username}, #{password}, 
         DATE_FORMAT(NOW(), '%Y-%m-%d %H:%i'), 
         DATE_FORMAT(NOW(), '%Y-%m-%d %H:%i'), 
         #{createdBy}, #{updatedBy})
    </insert>

    <!-- 更新配置 -->
    <update id="update">
        UPDATE code_generator_database_config
        SET name = #{name},
            type = #{type},
            host = #{host},
            port = #{port},
            `database` = #{database},
            `schema` = #{schema},
            username = #{username},
            password = #{password},
            updated_at = DATE_FORMAT(NOW(), '%Y-%m-%d %H:%i'),
            updated_by = #{updatedBy}
        WHERE id = #{id}
    </update>

    <!-- 删除配置 -->
    <delete id="deleteById">
        DELETE FROM code_generator_database_config
        WHERE id = #{id}
    </delete>

</mapper>

