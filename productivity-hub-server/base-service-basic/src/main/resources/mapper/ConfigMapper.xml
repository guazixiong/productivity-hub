<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pbad.config.mapper.ConfigMapper">

    <resultMap id="ConfigResultMap" type="com.pbad.config.domain.po.ConfigItemPO">
        <id property="id" column="id"/>
        <result property="module" column="module"/>
        <result property="configKey" column="config_key"/>
        <result property="configValue" column="config_value"/>
        <result property="description" column="description"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="updatedBy" column="updated_by"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, module, config_key, config_value, description, created_at, updated_at, updated_by
    </sql>

    <select id="selectAll" resultMap="ConfigResultMap">
        select
        <include refid="Base_Column_List"/>
        from sys_config
        order by created_at desc
    </select>

    <select id="selectById" parameterType="string" resultMap="ConfigResultMap">
        select
        <include refid="Base_Column_List"/>
        from sys_config
        where id = #{id}
        limit 1
    </select>

    <select id="selectByModuleAndKey" resultMap="ConfigResultMap">
        select
        <include refid="Base_Column_List"/>
        from sys_config
        where module = #{module}
          and config_key = #{configKey}
        limit 1
    </select>

    <update id="updateConfig" parameterType="com.pbad.config.domain.po.ConfigItemPO">
        update sys_config
        set
            module = #{module},
            config_key = #{configKey},
            config_value = #{configValue},
            description = #{description},
            updated_at = now(),
            updated_by = #{updatedBy}
        where id = #{id}
    </update>

    <insert id="insertConfig" parameterType="com.pbad.config.domain.po.ConfigItemPO">
        insert into sys_config
        (id, module, config_key, config_value, description, created_at, updated_at, updated_by)
        values
        (#{id}, #{module}, #{configKey}, #{configValue}, #{description}, now(), now(), #{updatedBy})
    </insert>
</mapper>

