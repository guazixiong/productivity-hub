<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pbad.codegenerator.mapper.CompanyTemplateMapper">

    <resultMap id="BaseResultMap" type="com.pbad.codegenerator.domain.po.CompanyTemplatePO">
        <id column="id" property="id" jdbcType="VARCHAR"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="description" property="description" jdbcType="VARCHAR"/>
        <result column="base_package" property="basePackage" jdbcType="VARCHAR"/>
        <result column="author" property="author" jdbcType="VARCHAR"/>
        <result column="templates_json" property="templatesJson" jdbcType="LONGVARCHAR"/>
        <result column="created_at" property="createdAt" jdbcType="VARCHAR"/>
        <result column="updated_at" property="updatedAt" jdbcType="VARCHAR"/>
        <result column="created_by" property="createdBy" jdbcType="VARCHAR"/>
        <result column="updated_by" property="updatedBy" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, name, description, base_package, author, templates_json, created_at, updated_at, created_by, updated_by
    </sql>

    <!-- 查询所有模板 -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM code_generator_company_template
        ORDER BY created_at DESC
    </select>

    <!-- 根据ID查询模板 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM code_generator_company_template
        WHERE id = #{id}
    </select>

    <!-- 插入模板 -->
    <insert id="insert">
        INSERT INTO code_generator_company_template 
        (id, name, description, base_package, author, templates_json, created_at, updated_at, created_by, updated_by)
        VALUES 
        (#{id}, #{name}, #{description}, #{basePackage}, #{author}, #{templatesJson}, 
         DATE_FORMAT(NOW(), '%Y-%m-%d %H:%i'), 
         DATE_FORMAT(NOW(), '%Y-%m-%d %H:%i'), 
         #{createdBy}, #{updatedBy})
    </insert>

    <!-- 更新模板 -->
    <update id="update">
        UPDATE code_generator_company_template
        SET name = #{name},
            description = #{description},
            base_package = #{basePackage},
            author = #{author},
            templates_json = #{templatesJson},
            updated_at = DATE_FORMAT(NOW(), '%Y-%m-%d %H:%i'),
            updated_by = #{updatedBy}
        WHERE id = #{id}
    </update>

    <!-- 删除模板 -->
    <delete id="deleteById">
        DELETE FROM code_generator_company_template
        WHERE id = #{id}
    </delete>

</mapper>

