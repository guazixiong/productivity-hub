<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pbad.todo.mapper.TodoEventMapper">

    <resultMap id="TodoEventResultMap" type="com.pbad.todo.domain.po.TodoEventPO">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="todoId" column="todo_id"/>
        <result property="eventType" column="event_type"/>
        <result property="occurredAt" column="occurred_at"/>
        <result property="payload" column="payload"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insertEvent" parameterType="com.pbad.todo.domain.po.TodoEventPO">
        INSERT INTO todo_event (id, user_id, todo_id, event_type, occurred_at, payload, created_at)
        VALUES (#{id}, #{userId}, #{todoId}, #{eventType}, #{occurredAt}, #{payload},
                IFNULL(#{createdAt}, NOW()))
    </insert>

    <select id="selectByTodoId" resultMap="TodoEventResultMap">
        SELECT id, user_id, todo_id, event_type, occurred_at, payload, created_at
        FROM todo_event
        WHERE todo_id = #{todoId}
          AND user_id = #{userId}
        ORDER BY occurred_at ASC
    </select>
</mapper>

